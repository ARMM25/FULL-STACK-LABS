import {Customer} from './customer' //set the import statement, the class and the file path

let customer = new Customer("John","Smith",20); //create the object using instance of Customer module
customer.greeter(); //call the greeter method
customer.GetAge(); //call the getAge method
