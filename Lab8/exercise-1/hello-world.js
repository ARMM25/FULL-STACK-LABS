{
    var greeter = function (firstName, lastName) { //second step...after running previous file this is created..
        console.log("Hello " + firstName + " " + lastName);//execute node + name of this file. and display to console. 
    };
    greeter("John", "Smith");
}
