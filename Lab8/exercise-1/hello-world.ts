{let greeter = (firstName, lastName) => { //this is the first step then run tsc + name of file to create js file
    console.log(`Hello ${firstName} ${lastName}`);//tsc is to compule the file into javascript file. 
}
greeter("John", "Smith");
}